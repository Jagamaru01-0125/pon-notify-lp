# AGENTS.md — Codex作業ルール(ぽん通知LP)

このリポジトリは「ぽん通知」需要検証LP専用です。通常は Claude Code / Fable5 が司令塔、Codex は実行役です。Claude Code が使えない間だけ、Codex は限定的な司令塔＋実行役として、小さな改善を進めてください。大方針・仕様・世界観は変えません。

## リポジトリ構成

- LP本体: `lp-v2/index.html`
- CSS/JS: `lp-v2/web/`
- `lp-v2/web/lp.css` = サイト全体、`mocks.css` = 電話モック系、`lp.js` = インタラクション
- 静的HTML / CSS / vanilla JS 構成。Next.js / Tailwind / build工程は廃止済み
- 画像はWebPを基本とする。新規画像追加・削除は明確な指示がある場合のみ
- アプリ実装コード、バックエンド、Push通知、認証、課金、チャット、位置情報共有は絶対に追加しない

## 作業開始時

作業フォルダ違いを防ぐため、最初に必ず確認してください。

```bash
pwd
git status -sb
git branch --show-current
git worktree list
```

実装前に短く出すこと: 現在の作業フォルダ、Git状態、主要ファイル、今回直す問題、変更予定ファイル、作業手順、確認方法。

作業ログが必要な場合は `docs/worklog/pon-lp-progress.md` に簡潔に残します。不要なログファイルは増やさないでください。

## プロダクト仕様(違反禁止)

- 主役は常に「子犬や小さな動物が、しぐさで合図を届ける体験」。通知カード・UIモック・機能説明だけを主役にしない
- 合図は5種のみ: 帰るよ / 着いた / 大丈夫？ / 見て / すき
- 通知文言は「○○さんから子犬が走ってきました」形式
- 既読ではなく「なでる」。履歴は「お届け履歴」
- ペア登録は1人だけ。位置情報・グループ・複数人機能は出さない
- チャットUI、吹き出し、入力欄、既読表示、「トーク」「メッセージ」というUI文言は禁止
- 家族管理アプリ風、Apple Watch専用アプリ風に寄せない

## デザイン規約

- 目標はAIっぽくない、高級感・温かさ・信頼感・かわいさがあるLP
- スマホ表示、余白と密度、世界観の統一、体験が伝わるスクロール演出を重視
- テンプレ感、量産LP感、安っぽい装飾、スカスカな余白、画像を貼っただけの構成、過剰なアニメーションは禁止
- 色は `lp.css` の既存変数を優先: `--bg`, `--bg2`, `--card`, `--ink`, `--ink2`, `--mut`, `--paw`, `--paw2`, `--line`
- フォントは `--font-head`(Zen Maru Gothic) / `--font-body`(Zen Kaku Gothic New)
- 影は茶系 `rgba(122,82,44,...)` を基本にする
- デスクトップ1280pxとモバイル390pxの両方を意識する

## CSS / JS ルール

- CSSは既存の変数、`clamp()`、余白設計に合わせる
- 同一要素への多重上書き、その場しのぎの `!important`、既存設計と違う色の直書き、無意味な新規クラス乱立を避ける
- JSは既存スタイルに合わせる: IIFE内、`var`、`/* ----- 名前 ----- */` コメント
- スクロールリビールは `.rv` / `.rv-host` と `lp.js` の IntersectionObserver (`.in`付与) を再利用する
- すべてのアニメーション・transitionは `@media (prefers-reduced-motion: reduce)` で無効化できるようにする
- hover効果は `@media (hover: hover)` 内に置く

## Codex単独作業時

Claude Code / Fable5 が止まっている間は、Codexが小さく作業して構いません。

Codexが判断してよいこと:

- 余白、CSS、レスポンシブ崩れ、hover / transition、既存スクロールリビール、軽微な文言違和感、重複CSS、明らかな表示崩れの修正

勝手に変更しないこと:

- LP全体構成の大幅変更、セクションの大幅な追加・削除、コンセプト変更、合図候補変更、「なでる」「お届け履歴」の廃止、チャットUI化、位置情報・グループ・複数人機能追加、新技術スタック導入、Next.js / Tailwind復帰、大量の画像追加・削除、デザイン方向性変更

迷う場合は実装せず、提案だけ出してください。1回の作業は基本1〜3ファイルに留めます。

## 作業後

必ず報告すること: 変更したファイル、変更内容、意図、差分要約、確認したコマンド、残課題、次にやる小さな作業。

可能なら実行してください。

```bash
git status -sb
git diff --stat
```

ビルド工程はありません。ブラウザ確認が難しい場合は、確認が必要な箇所を明記してください。

## 完了条件

ぽん通知の世界観を壊していないこと。子犬や小さな動物が合図を届ける体験が弱くなっていないこと。チャットアプリ風、Apple Watch専用アプリ風、画像を貼っただけのLPになっていないこと。スマホ表示が大きく崩れていないこと。変更が小さく、Fable5復旧後にレビューしやすいこと。
