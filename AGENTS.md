# AGENTS.md — Codex作業ルール(ぽん通知LP)

このリポジトリは「ぽん通知」需要検証LP専用。司令塔(Claude/Fable5)の指示書に従って小さな作業単位で実装すること。指示書にない変更は行わない。

## リポジトリ構成
- LP本体: `lp-v2/index.html` + `lp-v2/web/`(静的HTML/CSS/vanilla JS・ビルド工程なし。Next.js/Tailwindは廃止済み)
- `lp-v2/web/lp.css` = サイト全体 / `mocks.css` = 電話モック系 / `lp.js` = インタラクション
- 画像はWebP。新規画像追加・削除は指示がある場合のみ
- アプリ実装コード・バックエンド・Push通知・認証・課金・チャット・位置情報共有は絶対に追加しない

## プロダクト仕様(違反禁止)
- 主役は常に「子犬や小さな動物が、しぐさで合図を届ける体験」。通知カード・UIモックを主役にしない
- 合図は5種のみ: 帰るよ / 着いた / 大丈夫？ / 見て / すき(5文字以内)
- 通知文言は「○○さんから子犬が走ってきました」形式
- 既読ではなく「なでる」。履歴は「お届け履歴」
- チャットUI(吹き出し・入力欄・既読表示)を作らない。「トーク」「メッセージ」という語をUIに使わない
- ペア登録は1人だけ。位置情報・グループ・複数人機能は出さない

## デザイン規約
- 色は lp.css の変数(--bg, --bg2, --card, --ink, --ink2, --mut, --paw, --paw2, --line 等)のみ。原色・黒背景・強い光沢禁止
- フォント: --font-head(Zen Maru Gothic)/ --font-body(Zen Kaku Gothic New)
- すべてのアニメーション・transitionは @media (prefers-reduced-motion: reduce) で無効化
- hover効果は @media (hover: hover) 内に置く
- スクロールリビールは .rv / .rv-host + lp.js の IntersectionObserver(.in付与)を再利用
- 影は茶系 rgba(122,82,44,…)。デスクトップ1280pxとモバイル390pxの両方を考慮

## 作業様式
- JSは既存スタイルに合わせる(IIFE内・var・/* ----- 名前 ----- */ コメント)
- CSSは既存の変数・clamp()の密度感に合わせ、同一要素の多重上書きを作らない
- 完了時は変更ファイルと変更点を簡潔に報告。ブラウザでの見た目確認は司令塔が行う
