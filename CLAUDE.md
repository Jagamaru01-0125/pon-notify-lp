# ぽん通知 LP — プロジェクト指示(常時適用)

## 進め方(Fable5司令塔 × Codex実行役)
- Fable5(Claude)は司令塔。作業者ではない。担当: 目的理解 / コードベース把握 / LP構成・世界観・デザインのズレ発見 / 画像の使い方と優先順位の判断 / タスク分解 / Codexへの指示作成 / 実装結果レビュー / 最終品質判断
- 実装系の小タスクはサブエージェントではなく **Codex**(実行役・レビュー補助)に委任する: 画像整理・リネーム・未使用画像調査・CSS微調整・コンポーネント実装・モックHTML/CSS・スクロールアニメ実装・レスポンシブ調整・文言修正・alt追加・エラー対応・差分確認。レビュー補助は差分をプロンプトに渡して依頼(Codexサンドボックスはファイル読取りが不安定なため)
- Fable5が直接判断すること(委任禁止): LP全体構成 / セクション順と役割 / 画像の主役・不使用の選定 / 世界観適合 / 高級感 / 余白密度 / 演出の過剰さ / チャット・位置情報・Watch専用アプリに見えていないか / 需要検証LPとしての強さ
- 大規模な実装の前に「現状把握 → 提案 → 変更対象 → 作業分解」を簡潔に示してから着手する
- 進行ループ: 必要なファイルだけ読む → 方針決定 → 小さく分解 → Codexへ1回1作業単位で依頼 → Fable5がレビュー → 問題は更に小さくして再依頼 → 最後に全体品質・仕様・デザイン・表示を確認
- Codexへの指示には必ず含める: 作業目的 / 変更対象ファイル / やること / やらないこと / 世界観上の注意 / 完了条件 / 確認コマンド。「いい感じに直して」と投げない
- 禁止: Fable5の全実装抱え込み / Codexへの丸投げ / 全ファイル一気読み / 大きすぎる作業単位 / レビューなしの取り込み / 「buildが通る」だけでの完了判定
- 変更後は必ずローカルサーバー+Playwrightでデスクトップ(1280px)とモバイル(390px)を検証してからコミットする
- コミットは意味のある単位でまとめ、pushまで行う
- 完了報告に含める: Fable5の判断 / Codexへの委任内容と実装結果 / 変更ファイル / 使用画像・不使用画像と理由 / スクロール演出 / スマホ確認結果 / デプロイ・表示結果 / 残improvement

## プロダクト仕様(違反禁止)
- ぽん通知 = 大事な人1人に、ワンタップで5文字以内の合図を送り、子犬や小さな動物がしぐさで届けるアプリ
- LPの主役は常に「子犬や小さな動物が、しぐさで合図を届ける体験」。通知カードやUIモックを主役にしない
- 初期合図は5種: 帰るよ / 着いた / 大丈夫？ / 見て / すき(すべて5文字以内)
- 通知文言は「○○さんから子犬が走ってきました」形式
- 既読ではなく「なでる」。なでる通知は設定でオフ可能
- 履歴は「お届け履歴」。チャットUI(吹き出し・入力欄・既読)は絶対に作らない
- チャット / 位置情報共有 / 複数人登録 / グループ機能は主役にしない
- ペア登録は最初は1人だけ

## デザイン原則
- 色: ウォームホワイト / 淡いクリーム / やさしいベージュ / 上品なブラウン / 肉球ピンク / 柔らかいオレンジ
- 禁止: 原色・派手なピンクや黄色・黒背景の多用・強すぎる光沢・真っ白すぎる背景
- かわいいが、子どもっぽくしない。温かく上品で、少し高級感のあるWebサイトに仕上げる(高級感・温かさ・信頼感・世界観の統一を重視)
- **AIっぽい量産感を避ける**: テンプレ的な構成・ありきたりな表現・貼っただけの画像を排し、独自の体験演出を優先する
- 余白を取りすぎてスカスカにしない。テキスト・カード・モック・装飾の密度を設計する
- 画像は「貼っただけ」にせず、テキスト・スクロール演出と統合する
- 似た構図の犬画像・Watch画像を連続で使わない
- すべてのアニメーションは prefers-reduced-motion で無効化する
- スマホ表示(390px)の完成度を特に重視する

## リポジトリ構成
- LP本体: `lp-v2/index.html` + `lp-v2/web/`(静的HTML/CSS/JS・ビルド不要)
- 画像はWebP(旧PNGから変換済み)。新画像も追加時はWebP化を検討する
- ローカル確認: `python -m http.server 8765` → http://localhost:8765/lp-v2/
- デプロイ: Vercel静的配信(`vercel.json` で outputDirectory=lp-v2)
- 旧Next.js LPは削除済み。`npm run build` は存在しない(完了条件は「全セクション表示+コンソールエラー0+Vercelデプロイ成功」と読み替える)
- 完成条件はデプロイ成功だけではない: 世界観の統一 / スマホ表示 / 需要検証LPとしての強さ / **AIっぽさの少なさ** まで確認して完了とする
- 検証用スクリーンショットは `.playwright-mcp/` に保存する(gitignore済み)。リポジトリ直下に散らかさない
